# FitCoach Production Environment Configuration
# This file is for PRODUCTION BUILDS ONLY

# API Configuration
EXPO_PUBLIC_API_URL=https://api.fitcoach.com/api

# Environment
EXPO_PUBLIC_ENV=production

# Feature Flags
EXPO_PUBLIC_ENABLE_FIREBASE_DEV=false
EXPO_PUBLIC_ENABLE_DEBUG_LOGS=false

# AI Configuration
EXPO_PUBLIC_MAX_AI_REQUESTS=50

# OAuth Configuration
# CRITICAL: Add your production OAuth client IDs here
# Get from Google Cloud Console & Apple Developer Portal
# EXPO_PUBLIC_GOOGLE_CLIENT_ID_IOS=your-production-ios-client-id.apps.googleusercontent.com
# EXPO_PUBLIC_GOOGLE_CLIENT_ID_ANDROID=your-production-android-client-id.apps.googleusercontent.com

# ============================================================================
# IMPORTANT NOTES:
# ============================================================================
# 1. NEVER commit this file with real secrets to public repositories
# 2. Update EXPO_PUBLIC_API_URL to your actual production API domain
# 3. Ensure HTTPS is used for production API
# 4. Test thoroughly in staging environment before deploying
# 5. Keep separate .env files for:
#    - .env.development (local dev)
#    - .env.staging (staging server)
#    - .env.production (production server)
# ============================================================================

# ============================================================================
# DEPLOYMENT CHECKLIST:
# ============================================================================
# [ ] Update EXPO_PUBLIC_API_URL to production domain
# [ ] Disable debug logs (EXPO_PUBLIC_ENABLE_DEBUG_LOGS=false)
# [ ] Add production OAuth client IDs
# [ ] Test all features in staging first
# [ ] Verify Firebase is configured (GoogleService files)
# [ ] Check app.json has correct bundle identifiers
# [ ] Run: npx expo prebuild --clean
# [ ] Test on physical devices (iOS + Android)
# ============================================================================
